const firebase = require("firebase");
const { database } = firebase;
const Expo = require("expo-server-sdk");
let expo = new Expo();
let message =
  "emergency alert!!!!!!!!!! proceed to emergency safezone shown below";
exports.emergencyPush = (req, res) => {};
